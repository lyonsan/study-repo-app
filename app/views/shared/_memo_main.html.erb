<div class="memo-main-wrapper">
  <div class="memo-main-header">
    <h1 class="memo-main-header-title">メモ一覧</h1>
  </div>
  <div class="memo-search">
    <h1 class="memo-search-title">メモ検索</h1>
  </div>
  <div class="memo-contents">
    <ul class="memo-lists">
      <% @memos.each do |memo| %>
        <%= link_to "#" do %>
          <li class="memo-list">
            <div class="memo-topic">
              <%= memo.topic %>
            </div>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
</div>