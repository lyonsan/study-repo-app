<div class="report-top">
  <div class="report-user">
    <%= report.user.nickname %>
  </div>
  <div class="report-date">
    <%= l report.created_at, format: :short %>
  </div>
</div>
<div class="report-bottom">
  <div class="report-content">
    <h2>本日の学習時間</h2>
    <div class="report-input">
      <%= l report.study_time, format: :length %>
    </div>
  </div>
  <div class="report-content">
    <h2>集中して学習できた時間</h2>
    <div class="report-input">
      <%= l report.concentrated_time, format: :length %>
    </div>
  </div>
  <div class="report-content">
    <h2>本日用いた学習方略のうち、特によかったものとその理由</h2>
    <div class="report-input">
      <%= safe_join(report.good_way.split("\n"),tag(:br)) %>
    </div>
  </div>
  <div class="report-content">
    <h2>計画通りに学習できた？</h2>
    <div class="report-input">
      <% if report.achieved %>
        <p>はい</p>
      <% else %>
        <p>いいえ</p>
      <% end %>
    </div>
  </div>
  <% if report.go_wrong.present? %>
    <div class="report-content">
      <h2>(いいえの方)計画通りに学習できなかった理由</h2>
      <div class="report-input">
        <%= safe_join(report.go_wrong.split("\n"),tag(:br)) %>
      </div>
    </div>
  <% end %>
  <div class="report-content">
    <h2>明日の計画</h2>
    <div class="report-input">
      <%= safe_join(report.tomorrow_plan.split("\n"),tag(:br)) %>
    </div>
  </div>
  <div class="report-content">
    <h2>本日用いた学習方略のうち、特によかったものとその理由</h2>
    <div class="report-input">
      <%= safe_join(report.good_way.split("\n"),tag(:br)) %>
    </div>
  </div>
  <div class="report-content">
    <h2>本日学習した内容や知識</h2>
    <div class="report-input">
      <%= safe_join(report.study_content.split("\n"),tag(:br)) %>
    </div>
  </div>
  <div class="report-content">
    <h2>オススメの学習方略</h2>
    <div class="report-input">
      <%= safe_join(report.advice.split("\n"),tag(:br)) %>
    </div>
  </div>
</div>
